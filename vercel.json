{
  "buildCommand": "npm run build",
  "outputDirectory": "dist",
  "rewrites": [
    { "source": "/api/amap/(.*)", "destination": "/api/amap" },
    { "source": "/((?!api).*)", "destination": "/index.html" }
  ]
}
